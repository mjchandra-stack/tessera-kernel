# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 Jagadeesh Chandra Muddana <mjchandra@gmail.com>
#
# Builds only for the bare-metal ARM 32-bit platform; host builds skip it.
# Normative: docs/hardware/01-platform-and-cpu-support.md

load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "karch-arm32",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "tessera_karch_arm32",
    edition = "2024",
    target_compatible_with = [
        "@platforms//os:none",
        "@platforms//cpu:armv7",
    ],
    visibility = ["//kernel:__subpackages__"],
    deps = [
        "//kernel/karch",
        "//kernel/karch-arm-common",
    ],
)

filegroup(
    name = "srcs",
    srcs = glob(["**"]),
    visibility = ["//:__pkg__"],
)
